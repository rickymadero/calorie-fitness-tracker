# Copy to .env.local for local secrets (never commit .env.local).

NEXT_PUBLIC_SUPABASE_URL=
# Prefer sb_publishable_… with asymmetric ES256 JWT signing keys.
# Legacy anon JWTs (eyJ…) are still accepted but pair poorly with ES256 user tokens.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
